import { HorizontalHeroEntity, HorizontalHeroType } from './horizontal-hero.entity';
import { HorizontalHeroSection } from '@treenity/admin-components/elements';

export {
  HorizontalHeroSection as Render,
  HorizontalHeroEntity as Entity,
  HorizontalHeroType as EntityType,
};
export { default as schema } from './horizontal-hero.entity.schema.json';

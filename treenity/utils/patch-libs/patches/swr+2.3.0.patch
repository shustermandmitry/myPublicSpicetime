# generated by patch-package 6.4.14
#
# declared package:
#   swr: 2.3.0
#
diff --git a/node_modules/swr/dist/_internal/config-context-client-x_C9_NWC.mjs b/node_modules/swr/dist/_internal/config-context-client-x_C9_NWC.mjs
index 5803058..54aa696 100644
--- a/node_modules/swr/dist/_internal/config-context-client-x_C9_NWC.mjs
+++ b/node_modules/swr/dist/_internal/config-context-client-x_C9_NWC.mjs
@@ -49,10 +49,10 @@ const createCacheHelper = (cache, key)=>{
         state[6],
         // Get server cache snapshot
         ()=>{
-            if (!isUndefined(key)) {
-                // If the cache was updated on the client, we return the stored initial value.
-                if (key in INITIAL_CACHE) return INITIAL_CACHE[key];
-            }
+            // if (!isUndefined(key)) {
+            //     // If the cache was updated on the client, we return the stored initial value.
+            //     if (key in INITIAL_CACHE) return INITIAL_CACHE[key];
+            // }
             // If we haven't done any client-side updates, we return the current value.
             return !isUndefined(key) && cache.get(key) || EMPTY_CACHE;
         }
diff --git a/node_modules/swr/dist/index/index.mjs b/node_modules/swr/dist/index/index.mjs
index d00c3db..c96495c 100644
--- a/node_modules/swr/dist/index/index.mjs
+++ b/node_modules/swr/dist/index/index.mjs
@@ -575,7 +575,7 @@ const useSWRHandler = (_key, fetcher, config)=>{
         // without providing any fallback data. This causes hydration errors. See:
         // https://github.com/vercel/swr/issues/1832
         if (!IS_REACT_LEGACY && IS_SERVER) {
-            throw new Error('Fallback data is required when using Suspense in SSR.');
+            // throw new Error('Fallback data is required when using Suspense in SSR.');
         }
         // Always update fetcher and config refs even with the Suspense mode.
         fetcherRef.current = fetcher;
